/**
 * to make the projection work we need a 2:1 ratio of width and height
 *
 */
const width = 200;
const height = 112.5;
module.exports = { height, width }; // we need these in another file that's why
